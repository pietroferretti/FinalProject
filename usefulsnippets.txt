<!-- -->

			<div class='row'>
				<div class='col-lg-6'>

					<h3>Storing <small>Add some Schema</small></h3>
					<p>Let's add some schema information to our repository:</p>
					<textarea class="form-control" id="schema" class='turtle' style='width: 100%' rows='10'>
PREFIX owl:  &lt;http://www.w3.org/2002/07/owl#&gt; 
PREFIX rdf:  &lt;http://www.w3.org/1999/02/22-rdf-syntax-ns#&gt; 
PREFIX rdfs: &lt;http://www.w3.org/2000/01/rdf-schema#&gt;
PREFIX peter: &lt;http://www.semanticweb.org/pietro/peterOntology#&gt; 

peter:Pasta owl:equivalentClass peter:Rice .

</textarea>
					<p>Click the link button to <em>POST</em> the schema to our own triple store. We will wrap this call in a server side function <code>{{ url_for('store') }}</code></p>
					<div><a id="link12" class='btn btn-primary'>link</a></div>
				</div>
				<div class='col-lg-6'>
					<h3>Output</h3>
					<div id="linktarget12" class='well' style='height: 15em; scroll: both; overflow: auto;'>This is where the server response will appear.</div>
				</div>
			</div>

<!-- -->

			<div class='row'>
				<div class='col-lg-6'>
					<h3>Querying (without inferencing)</h3>
					<p>Let's run a query against the repository, do some client-side parsing of the JSON results, and show them in the <code>div</code>:</p>
					<textarea id='query14' class='sparql' style='width: 100%' rows='10'>
PREFIX owl:  &lt;http://www.w3.org/2002/07/owl#&gt; 
PREFIX rdf:  &lt;http://www.w3.org/1999/02/22-rdf-syntax-ns#&gt; 
PREFIX rdfs: &lt;http://www.w3.org/2000/01/rdf-schema#&gt;
PREFIX peter: &lt;http://www.semanticweb.org/pietro/peterOntology#&gt; 

SELECT ?x WHERE {
  ?x a peter:Course .
}
</textarea>
					<p>Click the link button to <em>GET</em> the query to our triple store. We will wrap this call in a server side function <code>{{ url_for('sparql') }}</code></p>
					<div><a id="link14" class='btn btn-primary'>link</a></div>
				</div>
				<div class='col-lg-6'>
					<h3>Output</h3>
					<div id="linktarget14" class='well' style='height: 15em; scroll: both; overflow: auto;'>This is where the server response will appear.</div>
				</div>
			</div>

			<div class='row'>
				<div class='col-lg-6'>
					<h3>Querying (with inferencing)</h3>
					<p>Let's run a query against the repository, do some client-side parsing of the JSON results, and show them in the <code>div</code>:</p>
					<textarea id='query15' class='sparql' style='width: 100%' rows='10'>
PREFIX owl:  &lt;http://www.w3.org/2002/07/owl#&gt; 
PREFIX rdf:  &lt;http://www.w3.org/1999/02/22-rdf-syntax-ns#&gt; 
PREFIX rdfs: &lt;http://www.w3.org/2000/01/rdf-schema#&gt;
PREFIX peter: &lt;http://www.semanticweb.org/pietro/peterOntology#&gt; 

SELECT ?x WHERE {
  ?x a peter:Course .
}
</textarea>
					<p>Click the link button to <em>GET</em> the query to our triple store. Again, we will wrap this call in a server side function <code>{{ url_for('sparql') }}</code></p>
					<div><a id="link15" class='btn btn-primary'>link</a></div>
				</div>
				<div class='col-lg-6'>
					<h3>Output</h3>
					<div id="linktarget15" class='well' style='height: 15em; scroll: both; overflow: auto;'>This is where the server response will appear.</div>
				</div>
			</div>

		</div>

		<script src='{{ url_for("static", filename="js/jquery-1.11.0.min.js") }}' type='text/javascript'></script>
		<script src='{{ url_for("static", filename="js/tutorial.js") }}' type='text/javascript'></script>


	</body>
</head>


/*
			var table = $('<table></table>');
            table.addClass('table');
            var thead = $('<thead></thead>');
            var tr1 = $('<tr></tr>');
			$.each(vars, function(index, value){
				tr1.append('<th>'+value+'</th>');
			})
			thead.append(tr1);
			table.append(thead);

			var tbody = $('<tbody></tbody>');
			$.each(json.results.bindings, function(index,value){
				var tr = $('<tr></tr>');
			
				$.each(vars, function(index, v){
					var v_type = value[v]['type'];
					var v_value = value[v]['value'];
				
					td = $('<td></td>');
				
					// If the value is a URI, create a hyperlink
					if (v_type == 'uri') {
						var a = $('<a></a>');
						a.attr('href',v_value);
						a.text(v_value.split('#')[1]);
						td.append(a);
					// Else we're just showing the value.
					} else {
						td.append(v_value);
					}
					tr.append(td);
				});
				tbody.append(tr);
			});
			table.append(tbody);

			$('#linktarget1').append(table);
*/